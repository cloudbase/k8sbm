apiVersion: bmc.tinkerbell.org/v1alpha1
kind: Job
metadata:
  name: job-power-reset-machine4
  namespace: tink-system
spec:
  machineRef:
    name: machine4
    namespace: tink-system
  tasks:
    - powerAction: "off"
    - powerAction: "on"
